using UnityEngine;
using System.Collections;

public class sayTag : MonoBehaviour {

	// Use this for initialization
	void Start () {
        Debug.Log(gameObject.tag);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
